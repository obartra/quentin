# assets/img

Drop real image files here using the exact filenames listed in
[`../../ASSETS.md`](../../ASSETS.md).

Until a file exists, its `<img>` tag removes itself and a labeled placeholder
shows in its place — so the layout stays intact while you gather assets.
